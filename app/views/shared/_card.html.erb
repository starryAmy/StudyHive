<% desk_obj = desk %> <%# this `desk` refers to OpenStruct %>
<% d = desk_obj.desk %>
<% status = desk_obj.status %>

<div class="index-card col-3" data-controller="message" style="background-image: url('<%= asset_path("desk2.png")%>');" data-message-target="clickable-card" data-action="click->message#toIndividualDesk" data-url="<%= desk_path(d) %>">
  <div class="card-title"><%= d.title %></div>
  <div class="card-username"><%= "@#{d.user.username}" %> | <%=d.points%> points <%= status %> </div>
  <div class="card-tasks">📚Interest: <%= d.interest %></div>
  <% if current_user %>
    <% if current_user.favorited?(d) %>
      <div class="desk-index-status">🐝 Followed</div>
    <% end %>
  <% end %>
</div>
